// Конфигурация системы коллизии
export const collisionConfig = {
  // Параметры зоны коллизии биты
  bat: {
    radius: 170, // Радиус коллизии в пикселях (относительно игрового контейнера 1920x1080)
    offsetX: 0, // Смещение зоны коллизии по X относительно центра биты
    offsetY: 30, // Смещение зоны коллизии по Y относительно центра биты
  },
  
  // Параметры коллизии шара
  ball: {
    radius: 15, // Радиус шара в пикселях
  },
  
  // Параметры отскока
  rebound: {
    speedMultiplier: 0.5, // Множитель скорости отскока (0.5 = 50% от исходной скорости)
    ease: 'none', // Тип анимации отскока ('none' для линейного, 'power4.out' для плавного)
    duration: 0.5, // Базовая длительность отскока в секундах
  },
  
  // Параметры взмаха биты
  swing: {
    duration: {
      back: 0.05, // Длительность взмаха назад в секундах
      forward: 0.04, // Длительность взмаха вперёд в секундах
      return: 0.1, // Длительность возврата в секундах
    },
    totalDuration: 200, // Общая длительность взмаха в миллисекундах
  },
  
  // Параметры визуальной (невидимой) биты
  batVisual: {
    length: 208,    // Длина биты (px) — CSS height
    width: 16,      // Ширина биты (px) — CSS width
    top: 614,       // Вертикальная позиция (px) — CSS top
    left: 305,      // Горизонтальная позиция (px) — CSS left
    initialAngle: -154, // Начальный угол (deg)
    swingAngle: -308    // Угол момента удара (deg)
  },

  // Отладочные параметры
  debug: {
    showCollisionZone: true, // Показывать зону коллизии
    showBatCenter: true, // Показывать центр биты
    showBallCenter: false, // Показывать центр шара
  }
};

export default collisionConfig;
